import { FloatingCards, TextFloatingCards } from "@/ui";

const SectionRevenues = () => {
  return (
    <section className="w-full h-screen justify-center justify-items-center">
      <TextFloatingCards />
      <FloatingCards />
    </section>
  );
};

export default SectionRevenues;